package demo.wrappers;

import java.time.Instant;
import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;

import org.openqa.selenium.Alert;
import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.WebElement;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.support.ui.ExpectedConditions;
import org.openqa.selenium.support.ui.Select;
import org.openqa.selenium.support.ui.WebDriverWait;
import java.time.Duration;

public class Wrappers {
    /*
     * Write your selenium wrappers here
     * 
     */
    // ChromeDriver driver;

    // // public void initializeDriver(String driverPath) {
    // //     System.setProperty("webdriver.chrome.driver", driverPath);
    // //     driver = new ChromeDriver();
    // // }

    // public void openUrl(String url) {
    //     driver.get(url);
    // }

    // public void enterTextByXpath(String xpath, String text) {
    //     WebElement element = driver.findElement(By.xpath(xpath));
    //     element.sendKeys(text);
    // }

    // public void clickByXpath(String xpath) {
    //     WebElement element = driver.findElement(By.xpath(xpath));
    //     element.click();
    // }

    // public void selectByVisibleText(String xpath, String visibleText) {
    //     Select dropdown = new Select(driver.findElement(By.xpath(xpath)));
    //     dropdown.selectByVisibleText(visibleText);
    // }
    // public void printSucess(String xpath){
    //     WebElement successMessage = driver.findElement(By.xpath(xpath));
    //     String message= successMessage.getText();
    //     System.out.println(message);

    // }

    // public void closeDriver() {
    //     driver.quit();
    // }
}
